{{- if .Values.traefik.enabled }}
# Main Ingress for services
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: services-ingress
  namespace: {{ .Values.infraNamespace }}
  annotations:
    {{- if .Values.traefik.tls }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- if and .Values.traefik.certResolver (ne .Values.traefik.certResolver "false") }}
    traefik.ingress.kubernetes.io/router.tls.certresolver: {{ .Values.traefik.certResolver | quote }}
    {{- end }}
    {{- end }}
    {{- if .Values.externalDns.enabled }}
    external-dns.alpha.kubernetes.io/cloudflare-proxied: {{ .Values.externalDns.proxied | quote }}
    {{- if .Values.externalDns.target }}
    external-dns.alpha.kubernetes.io/target: {{ .Values.externalDns.target | quote }}
    {{- end }}
    {{- end }}
spec:
  ingressClassName: traefik
  rules:
    {{- if .Values.lldap.enabled }}
    - host: ldap{{ .Values.hostSuffix }}.{{ .Values.ingressDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lldap
                port:
                  number: 17170
    {{- end }}
    {{- if .Values.gatus.enabled }}
    - host: status{{ .Values.hostSuffix }}.{{ .Values.ingressDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gatus
                port:
                  number: 8080
    {{- end }}
    {{- if .Values.streamShare.enabled }}
    - host: tv{{ .Values.hostSuffix }}.{{ .Values.ingressDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: stream-share
                port:
                  number: 8080
    {{- end }}
    {{- if .Values.vaultwarden.enabled }}
    - host: vault{{ .Values.hostSuffix }}.{{ .Values.ingressDomain }}
      http:
        paths:
          - path: /notifications/hub
            pathType: Prefix
            backend:
              service:
                name: vaultwarden
                port:
                  number: 3012
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vaultwarden
                port:
                  number: 80
    {{- end }}
    {{- if .Values.vpn.enabled }}
    - host: vpn{{ .Values.hostSuffix }}.{{ .Values.ingressDomain }}
      http:
        paths:
          - path: /admin
            pathType: Prefix
            backend:
              service:
                name: headscale
                port:
                  name: ui
          - path: /
            pathType: Prefix
            backend:
              service:
                name: headscale
                port:
                  name: http
    {{- end }}
    - host: nas{{ .Values.hostSuffix }}.{{ .Values.ingressDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nas-external
                port:
                  number: {{ .Values.traefik.nasPort }}

---
# Suivi Ingress (separate namespace)
{{- if .Values.suiviItem.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: suivi-ingress
  namespace: {{ .Values.suiviItemNamespace }}
  annotations:
    {{- if .Values.traefik.tls }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- if and .Values.traefik.certResolver (ne .Values.traefik.certResolver "false") }}
    traefik.ingress.kubernetes.io/router.tls.certresolver: {{ .Values.traefik.certResolver | quote }}
    {{- end }}
    {{- end }}
    {{- if .Values.externalDns.enabled }}
    external-dns.alpha.kubernetes.io/cloudflare-proxied: {{ .Values.externalDns.proxied | quote }}
    {{- if .Values.externalDns.target }}
    external-dns.alpha.kubernetes.io/target: {{ .Values.externalDns.target | quote }}
    {{- end }}
    {{- end }}
spec:
  ingressClassName: traefik
  rules:
    - host: suivi{{ .Values.hostSuffix }}.{{ .Values.ingressDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: suivi-item
                port:
                  number: 80
{{- end }}

---
# Redirections Ingress
{{- $ingressNamespace := .Values.infraNamespace }}
{{- $ingressDomain := .Values.ingressDomain }}
{{- $hostSuffix := .Values.hostSuffix }}
{{- $proxied := .Values.externalDns.proxied }}
{{- $externalDnsEnabled := .Values.externalDns.enabled }}

{{- range $r := (list (dict "host" "resume" "mw" "redirect-cv-en") (dict "host" "cv" "mw" "redirect-cv-fr") (dict "host" "lkdn" "mw" "redirect-linkedin") (dict "host" "www" "mw" "redirect-www")) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: redirect-{{ .host }}
  namespace: {{ $ingressNamespace }}
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: {{ $ingressNamespace }}-{{ .mw }}@kubernetescrd
    {{- if $.Values.traefik.tls }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- end }}
    {{- if $externalDnsEnabled }}
    external-dns.alpha.kubernetes.io/cloudflare-proxied: {{ $proxied | quote }}
    {{- if $.Values.externalDns.target }}
    external-dns.alpha.kubernetes.io/target: {{ $.Values.externalDns.target | quote }}
    {{- end }}
    {{- end }}
spec:
  ingressClassName: traefik
  rules:
    - host: {{ .host }}{{ $hostSuffix }}.{{ $ingressDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lldap # Redirect target is handled by Middleware, not Backend
                port:
                  number: 17170
---
{{- end }}

# Root domain redirect (Proxied by default on Cloudflare)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: root-redirect
  namespace: {{ .Values.infraNamespace }}
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: {{ .Values.infraNamespace }}-redirect-portfolio@kubernetescrd
    {{- if .Values.traefik.tls }}
    traefik.ingress.kubernetes.io/router.tls: "true"
    {{- end }}
    {{- if .Values.externalDns.enabled }}
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    {{- if .Values.externalDns.target }}
    external-dns.alpha.kubernetes.io/target: {{ .Values.externalDns.target | quote }}
    {{- end }}
    {{- end }}
spec:
  ingressClassName: traefik
  rules:
    - host: {{ .Values.ingressDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lldap
                port:
                  number: 17170
{{- end }}
