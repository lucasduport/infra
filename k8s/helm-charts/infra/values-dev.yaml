# Overrides for dev environment
infraNamespace: infra-dev
suiviItemNamespace: suivi-item-dev

ingressDomain: dev.lucasduport.cc
isDev: true

traefik:
  enabled: false # Enable after confirming Traefik CRDs are available: kubectl get crd | grep traefik
